export type {
  SubmissionForValidationDTO,
  SubmissionStatsDTO,
  SubmissionsResponseDTO,
  ValidateSubmissionRequestDTO,
  ValidateSubmissionResponseDTO,
} from './validation.dto';

export type {
  SubmissionId,
  SubmissionForValidation,
  SubmissionMember,
  SubmissionStats,
  SubmissionStatus,
  SubmissionTeam,
  SubmissionsForValidation,
} from './validation.model';
